general{

}
background {
    monitor =
    path=/home/gn/Pictures/andrew-clifton-zEnGSmjyIK4-unsplash.png
    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_passes = 0 # 0 disables blurring
    brightness = 0.70
}

input-field {
    monitor =
    size = 200, 30
    outline_thickness = 5
    dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgb(8F70EA)
    inner_color = rgb(FFFFFF)
    font_color = rgb(8F70EA)
    fade_on_empty = false
    placeholder_text =  # Text rendered in the input box when it's empty.
    hide_input = false
    rounding = -1

    position = 0, 0
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date '+%I:%M %p')</span>"
    color = rgba(8F70EA)
    font_size = 50
    font_family = IosevkaTerm Nerd Font Mono

    position = -100, -35
    halign = right
    valign = top
}


#label {
#    monitor =
#    text = Enter password to unlock!
#    color = rgb(FFFFFF)
#    font_size = 13
#    font_family = IosevkaTerm Nerd Font Mono
#    position = 0, 160
#    halign = center
#    valign = bottom
#}

